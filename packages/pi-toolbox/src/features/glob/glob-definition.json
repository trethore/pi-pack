{
  "name": "glob",
  "label": "glob",
  "description": "Find files by glob pattern using rg --files -g.",
  "promptSnippet": "Find files by glob pattern",
  "promptGuidelines": ["Use glob for fast file discovery."],
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["pattern"],
    "properties": {
      "pattern": {
        "type": "string",
        "description": "Glob pattern to match file against."
      },
      "path": {
        "type": "string",
        "description": "Directory to search in. If omitted, the current working directory is used as a default."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "description": "Maximum number of files to return. If omitted, defaults to {{defaultLimit}}."
      },
      "noIgnore": {
        "type": "boolean",
        "description": "Ignore .gitignore and .ignore files with --no-ignore."
      },
      "hidden": {
        "type": "boolean",
        "description": "Show hidden files with --hidden."
      }
    }
  }
}
