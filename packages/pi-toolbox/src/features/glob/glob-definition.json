{
  "name": "glob",
  "label": "glob",
  "description": "Find files using ripgrep file discovery: rg --files -g '<pattern>' <path(s)...>.",
  "promptSnippet": "Find files by glob pattern(s)",
  "promptGuidelines": [
    "Use glob for fast file discovery.",
    ".git internals are always excluded from results."
  ],
  "parameters": {
    "type": "object",
    "additionalProperties": false,
    "required": ["patterns"],
    "properties": {
      "patterns": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "string" },
        "description": "Glob pattern(s) used to match files. Provide one or more patterns; each pattern is passed as `-g <pattern>`; use `!` for exclusions."
      },
      "paths": {
        "type": "array",
        "minItems": 1,
        "items": { "type": "string" },
        "description": "Path(s) to search in. Provide one or more directories. If omitted, the current working directory is used."
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "description": "Maximum number of files to return. If omitted, the default limit is {{defaultLimit}}."
      },
      "noIgnore": {
        "type": "boolean",
        "description": "Include files ignored by .gitignore, .ignore, or other ripgrep ignore rules. If true, passes `--no-ignore`. If omitted, defaults to false."
      },
      "visibleOnly": {
        "type": "boolean",
        "description": "Search only non-hidden files and directories. If omitted or false, hidden files are included by default."
      }
    }
  }
}
